<UserControl x:Class="V_Launcher.Views.AdHocLauncherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:V_Launcher.Views"
             xmlns:vm="clr-namespace:V_Launcher.ViewModels"
             xmlns:resources="clr-namespace:V_Launcher.Resources"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:AdHocLauncherViewModel}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Converters.xaml"/>
                <ResourceDictionary Source="RedWhiteTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Margin" Value="0,0,0,12"/>
                <Setter Property="Foreground" Value="{StaticResource PrimaryRedBrush}"/>
            </Style>

            <Style x:Key="LabelStyle" TargetType="TextBlock">
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="0,5,10,5"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            </Style>

            <Style x:Key="ButtonStyle" TargetType="Button">
                <Setter Property="Margin" Value="5"/>
                <Setter Property="Padding" Value="15,8"/>
                <Setter Property="MinWidth" Value="80"/>
                <Setter Property="Background" Value="{StaticResource PrimaryRedBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource PrimaryWhiteBrush}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="4"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource LightRedBrush}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="{StaticResource DarkRedBrush}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="{StaticResource MediumGrayBrush}"/>
                                    <Setter Property="Foreground" Value="{StaticResource DarkGrayBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{x:Static resources:AdHocResources.AdHocHeader}"
                   Style="{StaticResource HeaderTextStyle}"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <GroupBox Header="{x:Static resources:AdHocResources.AdHocCopyPasswordHeader}"
                          Padding="15"
                          Margin="0,0,0,15"
                          Style="{StaticResource RedWhiteGroupBoxStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{x:Static resources:AdHocResources.AdHocAccountLabel}"
                                   Style="{StaticResource LabelStyle}"/>
                        <ComboBox Grid.Column="1"
                                  ItemsSource="{Binding AvailableAccounts}"
                                  SelectedItem="{Binding SelectedClipboardAccount}"
                                  DisplayMemberPath="DisplayName"
                                  Style="{StaticResource RedWhiteComboBoxStyle}"/>
                        <Button Grid.Column="2"
                                Content="{x:Static resources:AdHocResources.AdHocCopyPasswordButton}"
                                Command="{Binding CopyPasswordCommand}"
                                Style="{StaticResource ButtonStyle}"/>
                    </Grid>
                </GroupBox>

                <GroupBox Header="{x:Static resources:AdHocResources.AdHocRunHeader}"
                          Padding="15"
                          Style="{StaticResource RedWhiteGroupBoxStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Text="{x:Static resources:AdHocResources.AdHocAccountLabel}"
                                   Style="{StaticResource LabelStyle}"/>
                        <ComboBox Grid.Row="0"
                                  Grid.Column="1"
                                  Grid.ColumnSpan="2"
                                  ItemsSource="{Binding AvailableAccounts}"
                                  SelectedItem="{Binding SelectedLaunchAccount}"
                                  DisplayMemberPath="DisplayName"
                                  Style="{StaticResource RedWhiteComboBoxStyle}"/>

                        <TextBlock Grid.Row="1"
                                   Text="{x:Static resources:AdHocResources.AdHocExecutableLabel}"
                                   Style="{StaticResource LabelStyle}"/>
                        <TextBox Grid.Row="1"
                                 Grid.Column="1"
                                 Text="{Binding ExecutablePath, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource RedWhiteTextBoxStyle}"/>
                        <Button Grid.Row="1"
                                Grid.Column="2"
                                Content="{x:Static resources:AdHocResources.AdHocBrowseButton}"
                                Command="{Binding BrowseExecutableCommand}"
                                Style="{StaticResource ButtonStyle}"/>

                        <TextBlock Grid.Row="2"
                                   Text="{x:Static resources:AdHocResources.AdHocArgumentsLabel}"
                                   Style="{StaticResource LabelStyle}"/>
                        <TextBox Grid.Row="2"
                                 Grid.Column="1"
                                 Grid.ColumnSpan="2"
                                 Text="{Binding Arguments, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource RedWhiteTextBoxStyle}"/>

                        <TextBlock Grid.Row="3"
                                   Text="{x:Static resources:AdHocResources.AdHocWorkingDirectoryLabel}"
                                   Style="{StaticResource LabelStyle}"/>
                        <TextBox Grid.Row="3"
                                 Grid.Column="1"
                                 Text="{Binding WorkingDirectory, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource RedWhiteTextBoxStyle}"/>
                        <Button Grid.Row="3"
                                Grid.Column="2"
                                Content="{x:Static resources:AdHocResources.AdHocBrowseButton}"
                                Command="{Binding BrowseWorkingDirectoryCommand}"
                                Style="{StaticResource ButtonStyle}"/>

                        <Button Grid.Row="4"
                                Grid.Column="2"
                                HorizontalAlignment="Right"
                                Content="{x:Static resources:AdHocResources.AdHocRunButton}"
                                Command="{Binding LaunchExecutableCommand}"
                                Style="{StaticResource ButtonStyle}"/>
                    </Grid>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Margin="0,15,0,0" HorizontalAlignment="Center">
            <ProgressBar IsIndeterminate="True"
                         Width="200"
                         Height="16"
                         Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                         Style="{StaticResource RedWhiteProgressBarStyle}"/>
            <TextBlock Text="{Binding StatusMessage}"
                       HorizontalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                        <Setter Property="Margin" Value="0,8,0,0"/>
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding StatusMessage}" Value="">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding HasError}" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource ErrorRedBrush}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
