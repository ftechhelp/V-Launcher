<UserControl x:Class="V_Launcher.Views.LauncherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:V_Launcher.Views"
             xmlns:vm="clr-namespace:V_Launcher.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:LauncherViewModel}">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <!-- Import converters and theme -->
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Converters.xaml"/>
                <ResourceDictionary Source="RedWhiteTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <!-- Styles for consistent UI -->
            <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="24"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Margin" Value="0,0,0,20"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="Foreground" Value="{StaticResource PrimaryRedBrush}"/>
            </Style>
            
            <Style x:Key="ExecutableButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="120"/>
                <Setter Property="Height" Value="120"/>
                <Setter Property="Margin" Value="15"/>
                <Setter Property="Background" Value="{StaticResource PrimaryWhiteBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource MediumGrayBrush}"/>
                <Setter Property="BorderThickness" Value="2"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8"
                                    Padding="10">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource OffWhiteBrush}"/>
                                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryRedBrush}"/>
                                    <Setter Property="BorderThickness" Value="3"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="{StaticResource LightGrayBrush}"/>
                                    <Setter Property="BorderBrush" Value="{StaticResource DarkRedBrush}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="{StaticResource OffWhiteBrush}"/>
                                    <Setter Property="BorderBrush" Value="{StaticResource DarkGrayBrush}"/>
                                    <Setter Property="Opacity" Value="0.6"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            
            <Style x:Key="ExecutableIconStyle" TargetType="Image">
                <Setter Property="Width" Value="48"/>
                <Setter Property="Height" Value="48"/>
                <Setter Property="Stretch" Value="Uniform"/>
                <Setter Property="RenderOptions.BitmapScalingMode" Value="HighQuality"/>
            </Style>
            
            <Style x:Key="ExecutableLabelStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="TextAlignment" Value="Center"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
                <Setter Property="Margin" Value="0,8,0,0"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                <Setter Property="MaxWidth" Value="100"/>
            </Style>
            
            <Style x:Key="AccountLabelStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="TextAlignment" Value="Center"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
                <Setter Property="Margin" Value="0,2,0,0"/>
                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                <Setter Property="MaxWidth" Value="100"/>
            </Style>
            
            <Style x:Key="EmptyStateStyle" TargetType="StackPanel">
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="40"/>
            </Style>
            
            <Style x:Key="EmptyStateTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                <Setter Property="TextAlignment" Value="Center"/>
                <Setter Property="Margin" Value="0,10"/>
            </Style>
            

            
            <!-- Default icon for executables without custom icons -->
            <DrawingImage x:Key="DefaultExecutableIcon">
                <DrawingImage.Drawing>
                    <DrawingGroup>
                        <GeometryDrawing Brush="#FF495057" Geometry="M12,2A2,2 0 0,1 14,4V20A2,2 0 0,1 12,22H4A2,2 0 0,1 2,20V8L8,2H12M13,9V3.5L18.5,9H13Z"/>
                    </DrawingGroup>
                </DrawingImage.Drawing>
            </DrawingImage>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Application Launcher" 
                   Style="{StaticResource HeaderTextStyle}"/>
        

        
        <!-- Main Content Area -->
        <ScrollViewer Grid.Row="2" 
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <Grid>
                <!-- Loading Indicator -->
                <Grid Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel HorizontalAlignment="Center" 
                                VerticalAlignment="Center"
                                Margin="40">
                        <ProgressBar IsIndeterminate="True" 
                                     Width="200" 
                                     Height="20"
                                     Margin="0,0,0,15"
                                     Style="{StaticResource RedWhiteProgressBarStyle}"/>
                        <TextBlock Text="Loading applications..." 
                                   HorizontalAlignment="Center"
                                   FontSize="14"
                                   Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </Grid>
                
                <!-- Executable Icons Grid -->
                <ItemsControl ItemsSource="{Binding ExecutableItems}"
                              Visibility="{Binding IsLoading, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" 
                                       HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.LaunchExecutableCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource ExecutableButtonStyle}">
                                <StackPanel>
                                    <!-- Executable Icon -->
                                    <Image Style="{StaticResource ExecutableIconStyle}">
                                        <Image.Source>
                                            <PriorityBinding>
                                                <Binding Path="Icon"/>
                                                <Binding Source="{StaticResource DefaultExecutableIcon}"/>
                                            </PriorityBinding>
                                        </Image.Source>
                                    </Image>
                                    
                                    <!-- Executable Name -->
                                    <TextBlock Text="{Binding DisplayName}" 
                                               Style="{StaticResource ExecutableLabelStyle}"/>
                                    
                                    <!-- Account Name -->
                                    <TextBlock Text="{Binding AccountDisplayName}" 
                                               Style="{StaticResource AccountLabelStyle}"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
                <!-- Empty State -->
                <StackPanel Style="{StaticResource EmptyStateStyle}"
                            Visibility="{Binding ExecutableItems.Count, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                    <TextBlock Text="📱" 
                               FontSize="48" 
                               HorizontalAlignment="Center"
                               Margin="0,0,0,20"/>
                    <TextBlock Text="No applications configured" 
                               Style="{StaticResource EmptyStateTextStyle}"/>
                    <TextBlock Text="Use the 'Manage Apps' section to add executable configurations" 
                               Style="{StaticResource EmptyStateTextStyle}"
                               FontSize="14"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>
        
        <!-- Launch Status -->
        <Border Grid.Row="3" 
                Background="{StaticResource OffWhiteBrush}"
                BorderBrush="{StaticResource MediumGrayBrush}"
                BorderThickness="0,1,0,0"
                Padding="15,10"
                Visibility="{Binding IsLaunching, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" 
                             Width="150" 
                             Height="16"
                             Margin="0,0,15,0"
                             Style="{StaticResource RedWhiteProgressBarStyle}"/>
                <TextBlock Text="{Binding StatusMessage}" 
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>