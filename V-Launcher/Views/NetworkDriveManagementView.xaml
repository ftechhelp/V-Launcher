<UserControl x:Class="V_Launcher.Views.NetworkDriveManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:V_Launcher.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000"
             d:DataContext="{d:DesignInstance Type=vm:NetworkDriveManagementViewModel}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Converters.xaml"/>
                <ResourceDictionary Source="RedWhiteTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Margin" Value="0,0,0,10"/>
                <Setter Property="Foreground" Value="{StaticResource PrimaryRedBrush}"/>
            </Style>

            <Style x:Key="LabelStyle" TargetType="TextBlock">
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Margin" Value="0,5,0,2"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            </Style>

            <Style x:Key="ButtonStyle" TargetType="Button">
                <Setter Property="Margin" Value="5"/>
                <Setter Property="Padding" Value="15,8"/>
                <Setter Property="MinWidth" Value="80"/>
                <Setter Property="Background" Value="{StaticResource PrimaryRedBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource PrimaryWhiteBrush}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Network Drive Configuration Management"
                   Style="{StaticResource HeaderTextStyle}"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="Configured Network Drives" Padding="10" Style="{StaticResource RedWhiteGroupBoxStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ListBox Grid.Row="0"
                             ItemsSource="{Binding Configurations}"
                             SelectedItem="{Binding SelectedConfiguration}"
                             Style="{StaticResource RedWhiteListBoxStyle}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding RemotePath}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                        <Button Content="Add New" Command="{Binding AddConfigurationCommand}" Style="{StaticResource ButtonStyle}"/>
                        <Button Content="Edit" Command="{Binding EditConfigurationCommand}" Style="{StaticResource ButtonStyle}"/>
                        <Button Content="Delete" Command="{Binding DeleteConfigurationCommand}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
                                    <Setter Property="Background" Value="{StaticResource ErrorRedBrush}"/>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <GroupBox Grid.Column="2"
                      Header="Network Drive Details"
                      Padding="15"
                      Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}"
                      Style="{StaticResource RedWhiteGroupBoxStyle}">
                <StackPanel>
                    <TextBlock Text="Display Name:" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource RedWhiteTextBoxStyle}"
                             Margin="0,2,0,8"/>

                    <TextBlock Text="Remote Path (UNC):" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding RemotePath, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource RedWhiteTextBoxStyle}"
                             Margin="0,2,0,8"/>

                    <TextBlock Text="Credential Account:" Style="{StaticResource LabelStyle}"/>
                    <ComboBox ItemsSource="{Binding AvailableAccounts}"
                              SelectedItem="{Binding SelectedAccount}"
                              DisplayMemberPath="DisplayName"
                              Style="{StaticResource RedWhiteComboBoxStyle}"
                              Margin="0,2,0,15"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="Save"
                                Command="{Binding SaveConfigurationCommand}"
                                Style="{StaticResource ButtonStyle}"/>
                        <Button Content="Cancel"
                                Command="{Binding CancelEditCommand}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
                                    <Setter Property="Background" Value="{StaticResource MediumGrayBrush}"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </GroupBox>
        </Grid>

        <TextBlock Grid.Row="2"
                   Text="{Binding ValidationMessage}"
                   Foreground="{StaticResource ErrorRedBrush}"
                   FontWeight="SemiBold"
                   Margin="0,10,0,0"
                   TextWrapping="Wrap"
                   Visibility="{Binding HasValidationError, Converter={StaticResource BoolToVisibilityConverter}}"/>
    </Grid>
</UserControl>
