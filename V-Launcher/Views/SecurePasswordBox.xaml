<UserControl x:Class="V_Launcher.Views.SecurePasswordBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="200">
    <PasswordBox x:Name="InternalPasswordBox"
                 PasswordChanged="OnPasswordChanged"
                 Background="{StaticResource PrimaryWhiteBrush}"
                 BorderBrush="{StaticResource MediumGrayBrush}"
                 BorderThickness="1"
                 Padding="8,4"
                 FontSize="12">
        <PasswordBox.Style>
            <Style TargetType="PasswordBox">
                <Style.Triggers>
                    <Trigger Property="IsFocused" Value="True">
                        <Setter Property="BorderBrush" Value="{StaticResource PrimaryRedBrush}"/>
                        <Setter Property="BorderThickness" Value="2"/>
                    </Trigger>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="BorderBrush" Value="{StaticResource LightRedBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </PasswordBox.Style>
    </PasswordBox>
</UserControl>