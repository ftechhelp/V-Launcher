<Window x:Class="V_Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:V_Launcher"
        xmlns:views="clr-namespace:V_Launcher.Views"
        xmlns:vm="clr-namespace:V_Launcher.ViewModels"
        mc:Ignorable="d"
        Title="{Binding ApplicationTitle}" 
        Height="700" 
        Width="1000"
        MinHeight="600"
        MinWidth="800"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}">
    
    <Window.Resources>
        <!-- Converters -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Data Templates for ViewModels -->
        <DataTemplate DataType="{x:Type vm:LauncherViewModel}">
            <views:LauncherView/>
        </DataTemplate>
        
        <DataTemplate DataType="{x:Type vm:CredentialManagementViewModel}">
            <views:CredentialManagementView/>
        </DataTemplate>
        
        <DataTemplate DataType="{x:Type vm:ExecutableManagementViewModel}">
            <views:ExecutableManagementView/>
        </DataTemplate>
        
        <!-- Styles -->
        <Style x:Key="NavigationButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Background" Value="#FF3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF2980B9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF21618C"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#FFBDC3C7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="StatusBarStyle" TargetType="StatusBar">
            <Setter Property="Background" Value="#FFF8F9FA"/>
            <Setter Property="BorderBrush" Value="#FFDEE2E6"/>
            <Setter Property="BorderThickness" Value="0,1,0,0"/>
        </Style>
        
        <Style x:Key="SettingsCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="CheckBoxBorder"
                                    Width="16"
                                    Height="16"
                                    Background="White"
                                    BorderBrush="#FF95A5A6"
                                    BorderThickness="1"
                                    CornerRadius="2"
                                    Margin="0,0,5,0"
                                    VerticalAlignment="Center">
                                <Path x:Name="CheckMark"
                                      Data="M 0 2 L 2 4 L 6 0"
                                      Stroke="#FF27AE60"
                                      StrokeThickness="2"
                                      Visibility="Collapsed"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="#FFF8F9FA"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#FF3498DB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="#FFECF0F1"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#FFBDC3C7"/>
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="#FFECF0F1"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#FFBDC3C7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Navigation Bar -->
        <Border Grid.Row="0" 
                Background="#FF2C3E50" 
                Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Navigation Buttons -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Content="Launcher" 
                            Command="{Binding ShowLauncherViewCommand}"
                            Style="{StaticResource NavigationButtonStyle}"/>
                    <Button Content="Manage Accounts" 
                            Command="{Binding ShowCredentialManagementViewCommand}"
                            Style="{StaticResource NavigationButtonStyle}"/>
                    <Button Content="Manage Apps" 
                            Command="{Binding ShowExecutableManagementViewCommand}"
                            Style="{StaticResource NavigationButtonStyle}"/>
                </StackPanel>
                
                <!-- Application Settings -->
                <StackPanel Grid.Column="2" 
                            Orientation="Horizontal" 
                            VerticalAlignment="Center"
                            Margin="20,0,0,0">
                    <TextBlock Text="Settings:" 
                               Foreground="White" 
                               VerticalAlignment="Center"
                               Margin="0,0,10,0"
                               FontWeight="SemiBold"/>
                    
                    <CheckBox Content="Start with Windows" 
                              IsChecked="{Binding SettingsViewModel.Settings.StartOnWindowsStart}"
                              Command="{Binding ToggleStartOnWindowsStartCommand}"
                              CommandParameter="{Binding IsChecked, RelativeSource={RelativeSource Self}}"
                              Style="{StaticResource SettingsCheckBoxStyle}"
                              Margin="0,0,15,0"/>
                    
                    <CheckBox Content="Start Minimized" 
                              IsChecked="{Binding SettingsViewModel.Settings.StartMinimized}"
                              Command="{Binding ToggleStartMinimizedCommand}"
                              CommandParameter="{Binding IsChecked, RelativeSource={RelativeSource Self}}"
                              Style="{StaticResource SettingsCheckBoxStyle}"
                              Margin="0,0,15,0"/>
                    
                    <CheckBox Content="Minimize on Close" 
                              IsChecked="{Binding SettingsViewModel.Settings.MinimizeOnClose}"
                              Command="{Binding ToggleMinimizeOnCloseCommand}"
                              CommandParameter="{Binding IsChecked, RelativeSource={RelativeSource Self}}"
                              Style="{StaticResource SettingsCheckBoxStyle}"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Main Content Area -->
        <Border Grid.Row="1" 
                Background="White"
                Padding="0">
            <ContentPresenter Content="{Binding CurrentViewModel}"/>
        </Border>
        
        <!-- Status Bar -->
        <StatusBar Grid.Row="2" 
                   Style="{StaticResource StatusBarStyle}">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <!-- Loading Indicator -->
                    <ProgressBar IsIndeterminate="True"
                                 Width="100"
                                 Height="16"
                                 Margin="0,0,10,0"
                                 Visibility="{Binding IsInitializing, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    
                    <!-- Status Message -->
                    <TextBlock Text="{Binding StatusMessage}"
                               VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#FF495057"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasError}" Value="True">
                                        <Setter Property="Foreground" Value="#FFE74C3C"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
